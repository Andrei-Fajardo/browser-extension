# Optional — without these, votes use in-memory storage (fine for local demo).
# For production on Vercel, create a free Upstash Redis database and paste:
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
